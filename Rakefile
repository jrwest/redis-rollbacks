begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "redis-rollbacks"
    gem.summary = ""
    gem.email = "jordan.west@westwardwd.com"
  end		     
rescue LoadError
  puts "Jeweler (or a dependency) not available. Install it with: sudo gem install jeweler"
end
